# 统一行尾为 LF，与 .prettierrc 的 endOfLine: lf 保持一致。
# 避免 Windows 上 core.autocrlf=true 把 LF 转成 CRLF 后，
# 被 prettier 改回 LF，造成每次 lint-staged 都产生无意义的整文件 diff。
* text=auto eol=lf

# 二进制资源不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
